#pragma once

#include "cdll_dll.h"

// Once all the Half Life weapons have gone, this should be converted to a WeaponId_e array.
extern const int WEAPON_HUD_SLOTS[MAX_WEAPON_SLOTS][MAX_WEAPON_POSITIONS];
